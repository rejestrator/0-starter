doctype html
html(lang=lang||site.lang||'en')
  head
    meta(charset='utf-8')
    meta(name='viewport' content='width=device-width, initial-scale=1')
    title #{title}
    link(rel='stylesheet' href='/css/bootstrap.min.css')
  body
    .container
      h1 #{title}
      != contents
